version: "2"
services:
  proxy:
    image: diogok/caddy
    ports:
      - 80:80
      - 443:443
    volumes:
      - ./Caddyfile:/etc/Caddyfile
      - ./index.html:/var/www/html/index.html
      - /opt/caddy:/root/.caddy
